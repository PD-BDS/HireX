# Global configuration toggles shared by the Streamlit app, CLI, and crews.
llm:
  default_model: gpt-4o-mini
  temperature: 0.0
planning:
  enable_query_manager: true
  enable_guardrails: true
memory:
  storage_dir: conversation_sessions
  crewai_storage_dir: crewai_storage
rag:
  chroma_path: chroma_vectorstore
  resumes_collection: resumes_collection
screening:
  default_top_k: 5
  guardrail_max_retries: 2
